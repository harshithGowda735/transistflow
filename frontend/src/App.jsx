import { BrowserRouter, Link, Navigate, Route, Routes, useLocation } from 'react-router-dom';
import Landing from './pages/Landing'; import Login from './pages/Login'; import PassengerDashboard from './pages/PassengerDashboard'; import LiveBus from './pages/LiveBus'; import OperatorDashboard from './pages/OperatorDashboard'; import ConductorMode from './pages/ConductorMode'; import './index.css';
function Shell({ children }) { const location = useLocation(); const dashboard = location.pathname !== '/'; return <><header className="topbar"><Link className="brand" to="/"><span className="brand-mark">✦</span> TransitPulse</Link>{dashboard && <nav><Link to="/passenger">Passenger</Link><Link to="/operator">Operations</Link><Link to="/conductor">Conductor</Link></nav>}<Link className="header-login" to="/login">{dashboard ? 'Switch role' : 'Sign in'} <span>→</span></Link></header>{children}</>; }
export default function App() { return <BrowserRouter><Shell><Routes><Route path="/" element={<Landing />} /><Route path="/login" element={<Login />} /><Route path="/passenger" element={<PassengerDashboard />} /><Route path="/live/:busNumber" element={<LiveBus />} /><Route path="/operator" element={<OperatorDashboard />} /><Route path="/conductor" element={<ConductorMode />} /><Route path="*" element={<Navigate to="/" replace />} /></Routes></Shell></BrowserRouter>; }
